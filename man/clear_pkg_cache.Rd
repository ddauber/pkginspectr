% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{clear_pkg_cache}
\alias{clear_pkg_cache}
\title{Clear downloaded package cache}
\usage{
clear_pkg_cache(type = c("session", "persistent"))
}
\arguments{
\item{type}{A string indicating which cache to clear. Must be one of:
\itemize{
\item \code{"session"}: temporary cache stored in \code{tempdir()} (default)
\item \code{"persistent"}: long-term cache stored in a platform-specific user
cache directory via \code{rappdirs::user_cache_dir("pkginspectr")}
}}
}
\value{
Used for side effects. Invisibly returns the path that was cleared,
or \code{NULL} if no cache was found.
}
\description{
Deletes the cache directory used by \code{pkginspectr} to store downloaded
CRAN and GitHub packages. This is useful for freeing up disk space or
forcing fresh downloads in future analyses.
}
\details{
You can choose to clear either the session-level cache (temporary files
created during the current R session) or the persistent cache (files
stored across sessions in a user-specific cache directory).
}
\examples{
# Clear session cache
clear_pkg_cache("session")

# Clear persistent cache
clear_pkg_cache("persistent")
}
